apiVersion: v1
kind: PersistentVolume
metadata:
  name: backup-{{ name }}-pv
  labels:
    pv-usage: backup-{{ name }}
spec:
  persistentVolumeReclaimPolicy: Delete
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  hostPath:
    path: /tmp/data/pv-backup/